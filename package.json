{
  "name": "lotus-prospecting",
  "version": "1.0.0",
  "description": "Lótus Business — Motor de Inteligência de Prospecção",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"node server.js\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend",
    "start": "node server.js",
    "install:all": "npm install && npm install --prefix frontend"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.0",
    "express": "^4.19.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
